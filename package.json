{
  "name": "a-module-with-babelrc",
  "version": "0.0.0",
  "description": "A module with a .babelrc that, if it is used by the build tool, will error",
  "license": "Apache-2.0",
  "repository": "goto-bus-stop/a-module-with-babelrc",
  "devDependencies": {
    "babel-core": "^6.26.0"
  },
  "scripts": {
    "test": "node test.js"
  }
}
